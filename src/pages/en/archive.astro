---
import ArchivePanel from "@components/ArchivePanel.svelte";
import I18nKey from "@i18n/i18nKey";
import { i18n } from "@i18n/translation";
import MainGridLayout from "@layouts/MainGridLayout.astro";
import { localeUrl } from "../../utils/url-utils";
import { getSortedPostsList } from "../../utils/content-utils";

const sortedPostsList = await getSortedPostsList("en");
---

<MainGridLayout title={i18n(I18nKey.archive, "en")} alternateUrl={localeUrl("/archive/", "zh")}>
    <ArchivePanel sortedPosts={sortedPostsList} lang="en" client:only="svelte"></ArchivePanel>
</MainGridLayout>
